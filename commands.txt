cd hrms/backend
cp .env.example .env   # set your DATABASE_URL
uv sync
uv run alembic revision --autogenerate -m "create lead_enquiry table"
uv run alembic upgrade head
uv run uvicorn app.main:app --reload

cd hrms/frontend
npm install
npm run dev