#consistent password : SuperAdmin456

cd hrms/backend
cp .env.example .env   # set your DATABASE_URL
uv sync
uv run alembic revision --autogenerate -m "create lead_enquiry table"
uv run alembic revision --autogenerate -m "create location and salesmen tables" -> dir alembic.ini
uv run alembic revision --autogenerate -m "create employee table"
uv run alembic upgrade head
uv run uvicorn app.main:app --reload

cd hrms/frontend
npm install
npm run dev

#to create users
   uv run python scripts/create_admin.py

#downgrade alembic
uv run alembic downgrade 23caa383e8fb
uv run alembic history
uv run alembic show head
